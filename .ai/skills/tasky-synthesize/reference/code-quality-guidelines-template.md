# Code Quality Guidelines: [Project Name]

> This document defines the code quality standards, naming conventions, and best practices for this project.
> It is generated by the `/synthesize` command and used by implementation workflows to ensure all new code is clean, readable, and aligned with the existing codebase.

---

## 📖 Readability & Understandability

### Naming Conventions

#### Files & Directories
- **Pattern**: [e.g., `kebab-case.ts`, `PascalCase.cs`, `snake_case.py`]
- **Examples from project**:
  - `[e.g., user-service.ts, UserController.cs, auth_middleware.py]`

#### Classes & Types
- **Pattern**: [e.g., PascalCase]
- **Prefix/Suffix conventions**: [e.g., interfaces prefixed with `I`, types suffixed with `Type`]
- **Examples from project**:
  ```
  [e.g., UserService, IUserRepository, CreateUserDto, UserResponseType]
  ```

#### Functions & Methods
- **Pattern**: [e.g., camelCase, snake_case]
- **Verb-first convention**: [e.g., `getUser`, `createOrder`, `validateInput`]
- **Examples from project**:
  ```
  [e.g., getUserById, handleWebhookEvent, parseConfigFile]
  ```

#### Variables & Constants
- **Variables**: [e.g., camelCase]
- **Constants**: [e.g., UPPER_SNAKE_CASE]
- **Boolean naming**: [e.g., prefix with `is`, `has`, `should`, `can`]
- **Examples from project**:
  ```
  [e.g., 
    const userId = '123';
    const MAX_RETRY_COUNT = 3;
    const isAuthenticated = true;
  ]
  ```

#### Enums
- **Enum name**: [e.g., PascalCase singular]
- **Enum values**: [e.g., PascalCase, UPPER_SNAKE_CASE]
- **Examples from project**:
  ```
  [e.g., 
    enum UserRole { Admin, Editor, Viewer }
    enum OrderStatus { PENDING, PROCESSING, COMPLETED }
  ]
  ```

### Code Structure
- **Maximum function length**: [e.g., Functions should not exceed ~30 lines; extract helpers if they do]
- **Maximum file length**: [e.g., Files should stay under 300 lines; split into modules if they grow]
- **Single Responsibility**: [Each function/class should do one thing well]
- **Nesting depth**: [e.g., Maximum 3 levels of nesting; extract to helper functions beyond that]

### Comments & Documentation
- **When to comment**: [e.g., Comments explain "why", not "what" — the code should be self-documenting]
- **Docstrings/JSDoc**: [e.g., Required for all public APIs, optional for internal functions]
- **Example**:
  ```
  [e.g.,
    // BAD: Increment counter
    counter++;
    
    // GOOD: Retry count tracks failed webhook deliveries for exponential backoff
    retryCount++;
  ]
  ```

---

## 🔧 Maintainability & Modifiability

### Code Organization
- **Module structure**: [e.g., Feature-based organization vs. layer-based]
- **Import ordering**: [e.g., 1. External deps, 2. Internal modules, 3. Relative imports, separated by blank lines]
- **Export patterns**: [e.g., Named exports preferred over default exports]
- **Example**:
  ```
  [e.g.,
    // 1. External dependencies
    import express from 'express';
    import { z } from 'zod';
    
    // 2. Internal modules
    import { UserService } from '@/services/UserService';
    
    // 3. Relative imports
    import { validateBody } from './middleware';
  ]
  ```

### DRY (Don't Repeat Yourself)
- [e.g., Extract shared logic into utility functions in `src/utils/`]
- [e.g., Use base classes or mixins for shared behavior across similar components]
- [e.g., Configuration values should be centralized, not scattered across files]

### SOLID Principles
- **Single Responsibility**: [How the project applies this]
- **Open/Closed**: [How the project applies this]
- **Dependency Inversion**: [How the project applies this]

---

## ✅ Reliability & Correctness

### Error Handling
- **Pattern**: [e.g., Try-catch with typed errors, Result pattern, Either monad]
- **Error propagation**: [e.g., Throw in services, catch in controllers]
- **Logging**: [e.g., Log errors with context using structured logging]
- **Example**:
  ```
  [Code example from the project showing the standard error handling pattern]
  ```

### Input Validation
- **Where**: [e.g., At API boundaries, at service layer entry points]
- **Tool**: [e.g., Zod, Joi, class-validator, manual checks]
- **Example**:
  ```
  [Code example from the project showing standard input validation]
  ```

### Null Safety
- **Approach**: [e.g., Strict null checks enabled, Optional chaining preferred]
- [e.g., Never use `!` non-null assertion — handle null explicitly]
- [e.g., Use guard clauses for early returns on null/undefined]

---

## 🔒 Security

- [e.g., Never log sensitive data (passwords, tokens, PII)]
- [e.g., Always use parameterized queries — never string concatenation for SQL]
- [e.g., Validate and sanitize all user input before processing]
- [e.g., Use environment variables for secrets — never hardcode]
- [e.g., Keep dependencies up to date and audit regularly]
- [e.g., Use HTTPS for all external communications]

---

## 📏 Best Practices & Standards

### Language-Specific Standards
- [e.g., TypeScript: Enable `strict` mode, no `any` type]
- [e.g., Python: Follow PEP 8, use type hints for all public functions]
- [e.g., C#: Follow .NET naming conventions, use async/await for I/O]

### Formatting & Linting
- **Formatter**: [e.g., Prettier, Black, dotnet format]
- **Linter**: [e.g., ESLint, Pylint, Roslyn Analyzers]
- **Configuration location**: [e.g., `.eslintrc.js`, `pyproject.toml`]
- **Key rules enforced**:
  - [e.g., No unused variables]
  - [e.g., No console.log in production code]
  - [e.g., Maximum line length of 100 characters]

### Git & Version Control
- **Commit message format**: [e.g., Conventional Commits: `feat:`, `fix:`, `refactor:`, `docs:`]
- **Branch naming**: [e.g., `feature/description`, `fix/description`]
- **PR requirements**: [e.g., Must pass CI, must have code review]

---

## ⚠️ Anti-Patterns to Avoid

- [e.g., God classes / functions that do too many things]
- [e.g., Magic numbers / strings — use named constants]
- [e.g., Deep nesting — flatten with early returns or extraction]
- [e.g., Mutable global state — use dependency injection instead]
- [e.g., Ignoring errors silently — always handle or propagate]
- [e.g., Premature optimization — prioritize readability first]

---
*Last Updated: [YYYY-MM-DD]*
