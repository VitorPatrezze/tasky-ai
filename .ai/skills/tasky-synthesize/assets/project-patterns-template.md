# Project Patterns: [Project Name]

> This document catalogs all design patterns actively used in this project, with concrete examples, file locations, and guidance on how to extend each pattern.
> It is generated by the `/synthesize` command and used by implementation workflows to ensure new features align with established architectural decisions.

---

## 🏛️ Architectural Pattern Overview

- **Primary Architecture**: [e.g., MVC, Clean Architecture, Hexagonal, Layered, Event-Driven]
- **Key Principle**: [e.g., "All business logic lives in the domain layer, never in controllers"]
- **Layer Diagram**:
  ```
  [e.g.,
    Presentation → Application → Domain → Infrastructure
         ↓              ↓           ↓            ↓
    Controllers    Use Cases    Entities    Repositories
  ]
  ```

---

## 🔧 Design Patterns in Use

For each pattern found in the project, document the following:

---

### Pattern: [Pattern Name, e.g., Repository Pattern]

**Purpose**: [Why this pattern is used in this project]

**Where it is used**:
- `[file path, e.g., src/repositories/UserRepository.ts]`
- `[file path, e.g., src/repositories/OrderRepository.ts]`

**How it works** (concrete example from the project):
```
[Code example from an actual file in the project, e.g.:

export class UserRepository {
  constructor(private db: Database) {}

  async findById(id: string): Promise<User | null> {
    return this.db.collection('users').findOne({ _id: id });
  }

  async save(user: User): Promise<void> {
    await this.db.collection('users').updateOne(
      { _id: user.id },
      { $set: user },
      { upsert: true }
    );
  }
}
]
```

**How to extend this pattern** (guidance for adding new implementations):
```
[Step-by-step instructions, e.g.:

1. Create a new file in `src/repositories/` named `[Entity]Repository.ts`
2. Implement the base interface `IRepository<T>` from `src/repositories/interfaces.ts`
3. Inject the `Database` dependency via the constructor
4. Register the new repository in the DI container at `src/container.ts`
]
```

---

> **Repeat the above block for every design pattern found in the project.**
> Common patterns to look for:
> - Repository Pattern
> - Factory Pattern
> - Observer / Event Emitter Pattern
> - Strategy Pattern
> - Singleton Pattern
> - Dependency Injection
> - Builder Pattern
> - Adapter Pattern
> - Middleware / Pipeline Pattern
> - Command Pattern
> - State Machine Pattern
> - Decorator Pattern

---

## 🌐 API Patterns (if applicable)

### API Architecture
- **Style**: [e.g., REST, GraphQL, gRPC, WebSocket]
- **Base URL Structure**: [e.g., `/api/v1/[resource]`]
- **Versioning Strategy**: [e.g., URL path versioning, header versioning]

### Route / Endpoint Structure
- **Route Definition Location**: [e.g., `src/routes/`, `src/controllers/`]
- **Naming Convention**: [e.g., plural nouns for resources, `kebab-case`]
- **Example**:
  ```
  [e.g.,
    GET    /api/v1/users          → list all users
    GET    /api/v1/users/:id      → get single user
    POST   /api/v1/users          → create user
    PUT    /api/v1/users/:id      → update user
    DELETE /api/v1/users/:id      → delete user
  ]
  ```

### Request / Response Pattern
```
[Example showing the standard request handling flow, e.g.:

// Controller
export async function createUser(req: Request, res: Response) {
  const dto = validateBody(req.body, CreateUserSchema);
  const user = await userService.create(dto);
  return res.status(201).json({ data: user });
}
]
```

### Error Handling in API
```
[Example showing the standard API error response format, e.g.:

{
  "error": {
    "code": "USER_NOT_FOUND",
    "message": "No user found with ID 'abc-123'",
    "status": 404
  }
}
]
```

### How to Add a New API Endpoint
```
[Step-by-step guide, e.g.:

1. Define the route in `src/routes/[resource].routes.ts`
2. Create the controller in `src/controllers/[Resource]Controller.ts`
3. Create the service method in `src/services/[Resource]Service.ts`
4. Add request validation schema in `src/schemas/[resource].schema.ts`
5. Register the route in the main router at `src/routes/index.ts`
6. Add integration tests in `tests/integration/[resource].test.ts`
]
```

---

## 🔄 State Management Pattern (if applicable)

- **Approach**: [e.g., Redux, Zustand, Context API, Signals, MobX]
- **State Location**: [e.g., `src/store/`, `src/state/`]
- **How state flows**: [Describe the data flow from state to UI and back]
- **Example**:
  ```
  [Code example showing how state is created, updated, and consumed]
  ```
- **How to add new state**:
  ```
  [Step-by-step guide for adding a new piece of application state]
  ```

---

## 📦 Dependency Injection / Service Registration (if applicable)

- **DI Framework**: [e.g., InversifyJS, tsyringe, built-in, manual]
- **Registration Location**: [e.g., `src/container.ts`, `src/module.ts`]
- **Example**:
  ```
  [Code example showing how services are registered and resolved]
  ```

---

## ⚠️ Patterns to Preserve

> [!IMPORTANT]
> These patterns are intentional architectural decisions. New features MUST follow these established patterns rather than introducing alternatives.

- [e.g., All database access MUST go through repository classes — never query the DB directly from controllers]
- [e.g., All API responses MUST use the standard `{ data, error, meta }` envelope]
- [e.g., All events MUST be published through the EventBus — never use direct method calls for cross-module communication]

---
*Last Updated: [YYYY-MM-DD]*
