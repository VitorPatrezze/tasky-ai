description = "Maps the codebase and creates a single project reference file."
prompt = """
<objective>
Map the entire codebase using the `tasky-synthesize` skill to produce a comprehensive `.ai/CODE_REFERENCE.md` file.
</objective>

<process>
1. Activate and use the `tasky-synthesize` skill.
2. Follow its process to discover and analyze the tech stack, architecture, conventions, and testing patterns.
3. Produce the final `.ai/CODE_REFERENCE.md` file based on the provided template.
</process>

<success_criteria>
- `.ai/CODE_REFERENCE.md` is created in the `.ai/` directory.
- The document is comprehensive and includes file paths for every pattern.
- This is the ONLY file created during the process.
</success_criteria>
"""
